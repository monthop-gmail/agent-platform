# Code owners — ทำให้ authority ใน ADR-0006 มีผลจริง ไม่ใช่แค่เขียนในเอกสาร
#
# ⚠️ วันนี้ไฟล์นี้ยังไม่ให้ผลอะไรเลย และไม่ใช่เพราะลืมเปิดอะไร
#
# branch protection เปิดแล้วตั้งแต่ 2026-08-22 — required status check `drift`
# พร้อม enforce_admins · ทำให้ ADR-0006 ข้อ 3 (test fail = ปล่อยไม่ได้) มีผลจริง
#
# แต่ **ไม่ได้เปิด "Require review from Code Owners" โดยเจตนา** เพราะ repo นี้มี
# collaborator คนเดียว และ GitHub ไม่ให้อนุมัติ PR ของตัวเอง — เปิดแล้วจะ merge
# ไม่ได้เลยสักอัน ไม่ใช่ได้ gate ที่เข้มขึ้น
#
# ไฟล์นี้จะเริ่มทำงานเมื่อมี maintainer คนที่สอง หรือมีคนนอก fork มาเปิด PR
# — ตอนนั้นค่อยเปิด required code-owner review ได้โดยไม่ล็อกตัวเอง

# ค่าเริ่มต้น
*                       @monthop-gmail

# ผูกพัน — เปลี่ยนได้ผ่าน ADR เท่านั้น
/decisions/             @monthop-gmail
/contracts/             @monthop-gmail

# ทะเบียน consumer — กระทบการตัดสินใจปิด contract version
/architecture/consumers.md   @monthop-gmail

# บันทึกดิบ ไม่ผูกพัน — แก้ได้อิสระกว่า แต่ห้ามแก้เนื้อหา raw ย้อนหลัง
/ref/                   @monthop-gmail
